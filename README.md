# REST API little monsters (training with Spring Boot)

RESTful API with in **Java** using **Spring Boot** and **JPA**.

## docker-mysql8-phpmyadmin

Dockerized MySQL8 / PHPMyAdmin

### Run the container

`docker compose up -d`
